INSERT INTO games(
    id,
    serial_id,
    -- rom_id,
    developer_id,
    publisher_id,
    rating_id,
    users,
    franchise_id,
    release_year,
    release_month,
    region_id,
    genre_id,
    display_name,
    full_name,
    -- boxart_url,
    platform_id
) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)