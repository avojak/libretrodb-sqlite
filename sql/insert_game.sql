INSERT INTO games(
    id,
    serial_id,
    rom_id,
    developer_id,
    franchise_id,
    release_year,
    release_month,
    region_id,
    genre_id,
    description,
    display_name,
    -- boxart_url,
    platform_id
) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)